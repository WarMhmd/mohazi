# forbiddenDomains
Prevents the email from using specific blacklisted domains.

## Usage
Can be a single string or an array of strings.

```yaml
rules:
  forbiddenDomains: ["disposable.com", "spam.me"]
```
