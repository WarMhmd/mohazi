# Type: file

Description: Validates that the input corresponds to a file object. This is typically used for multipart/form-data validation.

Example:
```yaml
upload:
    avatar:
        type: file
```
