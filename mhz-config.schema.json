{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MHZ Config",
  "description": "Configuration for mohazi code generation.",
  "type": "object",
  "additionalProperties": false,
  "required": ["input", "languages"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional reference to the JSON schema for editor tooling.",
      "format": "uri-reference"
    },
    "input": {
      "type": "string",
      "minLength": 1,
      "description": "Path to the input directory containing .mhz sources (relative to the config file).",
      "examples": ["./mhz"]
    },
    "languages": {
      "type": "object",
      "description": "Per-language output configuration keyed by language name (e.g. C#, javascript).",
      "minProperties": 1,
      "patternProperties": {
        "javascript|C#": {
          "$ref": "#/$defs/languageConfig"
        }
      },
      "additionalProperties": false,
      "examples": [
        {
          "javascript": { "enabled": true, "output": "./frontend/lib/mhz" },
          "C#": { "enabled": true, "output": "./backend/lib/mhz" }
        }
      ]
    }
  },
  "$defs": {
    "languageConfig": {
      "type": "object",
      "description": "Configuration for a single language backend.",
      "additionalProperties": false,
      "required": ["output"],
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether this language backend is enabled."
        },
        "output": {
          "type": "string",
          "minLength": 1,
          "description": "Output directory for generated files (relative to the config file)."
        }
      }
    }
  },
  "examples": [
    {
      "input": "./mhz",
      "languages": {
        "javascript": {
          "enabled": true,
          "output": "./frontend/lib/mhz"
        },
        "C#": {
          "enabled": true,
          "output": "./backend/lib/mhz"
        }
      }
    }
  ]
}
